'use strict';

const nock = require('nock');
// const userModels = require('../models/users');

/** mock get user profile */
// nock('http://localhost')
// .get('/users/user')
// .reply(200, userModels.User);

// /** mock get user profile by id */
// nock('http://localhost')
// .get(/\/users\/user\/profile\/\d+/)
// .reply(200, userModels.User);

// /** mock user login */
// nock('http://localhost')
// .get('/users/login')
// .reply(200, userModels.Login);
